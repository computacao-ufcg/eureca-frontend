const navOptions = ['Discentes', 'Disciplinas', 'Matrículas'];

const nameNavOptions = ['students', 'subjects', 'enrollments'];

const studentsOptions = ['Ativos', 'Egressos', 'Evadidos', 'Retidos'];

const nameStudents = ['active', 'graduated', 'evaded', 'retained'];

const subjectsOptions = ['Sumário', 'Métricas'];

const nameSubjects = ['summary', 'metrics'];

export { navOptions, studentsOptions, subjectsOptions, nameStudents, nameSubjects, nameNavOptions };